import { useState, useRef } from "react"

const StopWatch = ()=>{
console.log(Date)
    
    return (
        <>
        <h1></h1>
        <button>Start</button>
        <button>stop</button>
        </>
    )
}

export default StopWatch